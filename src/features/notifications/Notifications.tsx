import { Box, Typography } from '@mui/material';

export const Notifications = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="body1">
        This is a simple placeholder for the Notifications component.
      </Typography>
    </Box>
  );
};
